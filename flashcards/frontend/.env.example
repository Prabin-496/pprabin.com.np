# Leave EMPTY in production — the flashcards API runs as Vercel functions on the
# same origin (/api/*), so no cross-origin base URL is needed.
# Set this only to point a local UI at an already-deployed API.
VITE_FLASHCARDS_API_URL=
