# Production: your EC2 public URL or API Gateway URL (no trailing slash)
# Local dev with Vite proxy: leave empty — requests go to /api via proxy
VITE_FLASHCARDS_API_URL=https://your-ec2-public-ip:4000
