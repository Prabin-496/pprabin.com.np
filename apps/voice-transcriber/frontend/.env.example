# Leave EMPTY in production — the Voice AI API runs as a Vercel function on the
# same origin (/api/recordings/*), so no cross-origin base URL is needed.
# Set this only to point a local UI at an already-deployed API.
VITE_VOICE_AI_API_URL=
